<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="hw_scale_inventory.list">
      <field name="name">hw_scale_inventory list</field>
      <field name="model">hw_scale_inventory.hw_scale_inventory</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="hw_scale_inventory.action_window">
      <field name="name">hw_scale_inventory window</field>
      <field name="res_model">hw_scale_inventory.hw_scale_inventory</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="hw_scale_inventory.action_server">
      <field name="name">hw_scale_inventory server</field>
      <field name="model_id" ref="model_hw_scale_inventory_hw_scale_inventory"/>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="hw_scale_inventory" id="hw_scale_inventory.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="hw_scale_inventory.menu_1" parent="hw_scale_inventory.menu_root"/>
    <menuitem name="Menu 2" id="hw_scale_inventory.menu_2" parent="hw_scale_inventory.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="hw_scale_inventory.menu_1_list" parent="hw_scale_inventory.menu_1"
              action="hw_scale_inventory.action_window"/>
    <menuitem name="Server to list" id="hw_scale_inventory" parent="hw_scale_inventory.menu_2"
              action="hw_scale_inventory.action_server"/>
    -->
  </data>
</odoo>